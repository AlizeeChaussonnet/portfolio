generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model projet {
  id          Int      @id @default(autoincrement())
  name        String?  @db.VarChar(255)
  picture     String?  @db.VarChar(500)
  link        String?  @db.VarChar(500)
  github      String?  @db.VarChar(500)
  description String?  @db.VarChar(255)
  explanation String?  @db.VarChar(500)
  technos     techno[]

}

// model projet_techno {
//   id_techno Int
//   id_projet Int
//   projet    projet @relation(fields: [id_projet], references: [id])
//   techno    techno @relation(fields: [id_techno], references: [id])

//   @@id([id_techno, id_projet])
// }

model techno {
  id          Int      @id @default(autoincrement())
  techno_name String   @db.VarChar(50)
  projets     projet[]
}
